
import { backgroundStyle } from 'ghost-desktop/helpers/background-style';
import { module, test } from 'qunit';

module('Unit | Helper | background style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = backgroundStyle([42]);
  assert.ok(result);
});

